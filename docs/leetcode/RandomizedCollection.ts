class RandomizedCollection {

  private _count = 0;

  dataSet: Map<number, number[]> = new Map();

  get size() {
    return this._count;
  }

  constructor() {}

  insert(val: number): boolean {

  }

  remove(val: number): boolean {

  }

  getRandom(): number {
    
  }
}
