import { integerReplacement } from "./integerReplacement";

describe("integerReplacement", () => {
  it("case 1", () => {
    const n = 8;
    const t = integerReplacement(n);
    console.log(t);
  });
});
